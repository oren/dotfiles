#!/bin/bash

# i3 run this file when x starts.

i3-msg 'workspace 1;exec chromium-browser'   # open chromium in workspace 1
xset r rate 200 60                           # typematic delay and rate
synclient TouchpadOff=1                      # turn off touchpad
xrdb ~/.Xresources
