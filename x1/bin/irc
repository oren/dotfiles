#!/bin/bash

# display weechat highlight on i3blocks

msg=$(ssh do "tail -n 1 ~/.weechat/highlights.txt | sed 's/[^(]\+(\w\+: \(.*\))$/\1/'")

if [[ $msg == freenode:* ]]; then
  msg=""
  # ssh do "rm ~/.weechat/highlights.txt"
fi

echo $msg
echo
echo \#f95f5f
