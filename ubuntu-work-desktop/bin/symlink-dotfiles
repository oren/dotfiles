#!/bin/bash

# BASH PROFILE
ln -vfs ~/.dotfiles/ubuntu-work-desktop/bashrc ~/.bashrc
ln -vfs ~/.dotfiles/ubuntu-work-desktop/bash_profile ~/.bash_profile
ln -vfs ~/.dotfiles/ubuntu-work-desktop/bash_aliases ~/.bash_aliases
ln -vfs ~/.dotfiles/ubuntu-work-desktop/xinitrc ~/.xinitrc
ln -vfs ~/.dotfiles/ubuntu-work-desktop/Xresources ~/.Xresources

# VIM FILES
if [ ! -L ~/.vim ]; then
  ln -vfs ~/.dotfiles/ubuntu-work-desktop/vim ~/.vim
else
	echo .vim is already a symlink
fi

ln -vfs ~/.dotfiles/ubuntu-work-desktop/vim/vimrc ~/.vimrc
ln -vfs ~/.dotfiles/ubuntu-work-desktop/vim/gvimrc ~/.gvimrc

# GEMRC
ln -vfs ~/.dotfiles/ubuntu-work-desktop/etc/gemrc ~/.gemrc
ln -vfs ~/.dotfiles/ubuntu-work-desktop/etc/irbrc ~/.irbrc

# SCREEN
ln -vfs ~/.dotfiles/ubuntu-work-desktop/etc/screenrc ~/.screenrc

ln -vfs ~/.dotfiles/ubuntu-work-desktop/tmux.conf ~/.tmux.conf

ln -vfs ~/.dotfiles/ubuntu-work-desktop/pentadactylrc ~/.pentadactylrc

if [ ! -L ~/.weechat ]; then
	ln -vfs ~/.dotfiles/ubuntu-work-desktop/weechat ~/.weechat
else
	echo .weechat is already a symlink
fi

if [ ! -L ~/.i3 ]; then
	ln -vfs ~/.dotfiles/ubuntu-work-desktop/i3 ~/.i3
else
	echo .i3 is already a symlink
fi

# GIT
ln -vfs ~/.dotfiles/ubuntu-work-desktop/git-completion ~/.git-completion
ln -vfs ~/.dotfiles/ubuntu-work-desktop/gitconfig ~/.gitconfig

# NPM
ln -vfs ~/.dotfiles/ubuntu-work-desktop/npmrc ~/.npmrc

# CREATE VIM BACKUP DIR
mkdir -p ~/.backup/vim/swap
